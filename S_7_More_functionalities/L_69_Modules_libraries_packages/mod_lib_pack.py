# modules, libraries, packages

# module - python file containing group of methods
import os

# library - group of modules
import sqlite3

# package - 3rd party library
# to install package type pip install <pack. name> in the terminal

import glob2

glob2.glob("*") # lists all files
glob2.glob("*.txt") # lists all txt files
